# 📦 建立 GitHub 儲存庫指南

## ✅ 當前狀態

- ✅ Git 倉庫已初始化
- ✅ 初始提交已完成（45 個檔案）
- ✅ 分支已設為 `main`

## 🚀 建立儲存庫的方法

### 方法 1：使用 GitHub CLI（最簡單）⭐ 推薦

#### 1.1 安裝 GitHub CLI

```bash
# macOS
brew install gh

# 或使用官方安裝腳本
curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
```

#### 1.2 登入 GitHub

```bash
gh auth login
```

按照提示完成認證（會開啟瀏覽器）。

#### 1.3 建立並推送儲存庫

```bash
# 使用自動化腳本
./create-repo.sh

# 或手動執行
gh repo create material-request-system \
  --description "叫料系統 - Material Request System" \
  --private \
  --source=. \
  --remote=origin \
  --push
```

### 方法 2：手動建立（無需安裝）

#### 2.1 在 GitHub 網站建立儲存庫

1. 前往：https://github.com/new
2. 輸入儲存庫資訊：
   - **Repository name**: `material-request-system`
   - **Description**: `叫料系統 - Material Request System`
   - **Visibility**: 選擇 Public 或 Private
   - **⚠️ 重要**：**不要**勾選以下選項：
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
3. 點選「Create repository」

#### 2.2 連接並推送程式碼

GitHub 會顯示指令，但您也可以執行：

```bash
# 使用自動化腳本（推薦）
./push-to-github.sh

# 或手動執行（替換成您的儲存庫 URL）
git remote add origin https://github.com/您的帳號/material-request-system.git
git push -u origin main
```

## 📝 詳細步驟

### 使用自動化腳本

```bash
# 方法 A：自動建立儲存庫（需要 GitHub CLI）
./create-repo.sh

# 方法 B：手動建立後推送
./push-to-github.sh
```

### 手動執行

```bash
# 1. 在 GitHub 網站建立儲存庫
# 前往 https://github.com/new

# 2. 複製儲存庫 URL（例如：https://github.com/username/repo.git）

# 3. 連接遠端儲存庫
git remote add origin https://github.com/您的帳號/儲存庫名稱.git

# 4. 推送到 GitHub
git push -u origin main
```

## 🔍 驗證

推送成功後，您應該看到：

```
Enumerating objects: XX, done.
Counting objects: 100% (XX/XX), done.
...
To https://github.com/您的帳號/material-request-system.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

## 📚 下一步

儲存庫建立完成後，請參考：
- `QUICK_DEPLOY.md` - 部署到 Render 的步驟

## ❓ 常見問題

### Q: 如果推送時要求輸入帳號密碼？
A: GitHub 已不再支援密碼驗證，請使用 Personal Access Token 或 SSH key。

### Q: 如何設定 SSH key？
```bash
# 1. 檢查是否已有 SSH key
ls -la ~/.ssh

# 2. 如果沒有，產生新的
ssh-keygen -t ed25519 -C "your_email@example.com"

# 3. 將公鑰加入 GitHub
cat ~/.ssh/id_ed25519.pub
# 複製內容，前往 GitHub Settings > SSH and GPG keys > New SSH key

# 4. 使用 SSH URL 連接
git remote set-url origin git@github.com:您的帳號/儲存庫名稱.git
```

### Q: 如何產生 Personal Access Token？
1. 前往：https://github.com/settings/tokens
2. 點選「Generate new token (classic)」
3. 設定權限：勾選 `repo`
4. 產生後複製 token
5. 推送時使用 token 作為密碼

